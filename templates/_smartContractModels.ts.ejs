import { BaseStorage } from '@worldsibu/convector-core-storage';
import { CouchDBStorage } from '@worldsibu/convector-storage-couchdb';
<% dto.forEach(function(innerDto){ %><% innerDto.models.forEach(function(model){ %>
import { <%= model %> as <%= model %>Model } from '<%= innerDto.chaincodeClientFolder %>';<% }); %><% }); %>

export namespace Models {<% dto.forEach(function(innerDto){ %><% innerDto.models.forEach(function(model){ %>
  export const <%= model %> = <%= model %>Model;<% }); %><% }); %>
}
